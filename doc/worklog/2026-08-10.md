# 15:53 对话记录：Android 沉浸式计时排障与清理

- 记录时间：2026-08-10 15:53
- 对话主题：修复 Android 16 沉浸式计时被启动图覆盖、应用启动闪退和横屏状态栏白条。
- 本次完成：
  - 通过 logcat 确认应用闪退来自 `FocusNotificationService` 的前台服务超时。
  - 通过 SurfaceFlinger、窗口状态和截图确认启动图来自 Activity 的 `android:background`，不是夜间模式、状态栏插件或第三方覆盖层。
  - 移除旧式全屏启动图背景并禁用 legacy preview，用户验证沉浸式计时可以正常进入。
  - 改造沉浸式状态恢复逻辑，在旋转与窗口焦点恢复后重新隐藏系统栏并设置黑色背景。
  - 清理临时日志、700ms 等待、四边遮罩层，以及已无调用点的辅助类和测试。
  - 已通过 `npm run build`、`npx cap sync android` 和沉浸式方向辅助测试（14/14）。
- 产出物：
  - `docs/lessons/2026-08-10-android-immersive-timer.md`
- 关键决策：
  - 保留启动主题和前台服务的有效修复。
  - 不再以固定延时或视觉遮罩处理 Android 固定旋转。
- 遗留问题：
  - 需在新 APK 上回归横屏顶端白条、横竖屏切换和恢复前台场景。
- 下一步：
  - 在 Android Studio 手动编译并安装最新 APK，完成上述回归检查。
