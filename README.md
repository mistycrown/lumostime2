# LumosTime 🌟

LumosTime 是一款专注于个人成长与生活记录的全能时间管理应用。它融合了**时间追踪**、**待办事项管理**、**领域目标 (Scope & Goals)**、**数据统计**、**AI 教练**以及**WebDAV 云同步**等功能，旨在通过精美的 UI 设计（莫兰迪色系）和流畅的交互体验，帮助用户将被动的时间流逝转化为主动的生命积累。

支持 **Web** 端访问与 **Android** 原生应用体验。

## ✨ 核心功能

### 1. ⏱️ 沉浸式时间记录 (Time Tracking)
- **多任务并行**：支持同时开始多个专注会话，灵活应对复杂场景。
- **悬浮计时球**：全局悬浮窗实时显示当前进行中的活动，支持快速结束或暂停。
- **智能补录**：便捷的时间块拖拽交互，支持双滑块精确调整时间，智能填补空白时间段。
- **极简交互**：点击图标即可开始/结束计时，无感记录。
- **专注度评分**：记录结束时可对本次专注质量进行 1-5 分打分，追踪状态波动。

### 2. 🎯 领域与目标系统 (Scope & Goals)
- **正交维度管理**：引入 **"Scope (领域)"** 概念（如：无论是在“工作”还是“学习”分类下，都可以属于“输入”或“输出”领域），提供跨分类的宏观视角。
- **目标追踪**：
    - 支持设定特定时间段（如 Q1、年度）的目标。
    - **多维度指标**：支持按“时长”、“数量 (Task Count)”、“专注度加权时长”、“活跃天数”等多种指标设定目标。
    - **自动进度**：目标与 Scope 关联，自动统计相关记录积累进度。

### 3. 📝 深度待办管理 (Advanced Todo)
- **任务关联**：将待办事项（Todo）与时间标签（Activity）关联，实现“做任务即记录时间”的自动化统计。
- **墨水进度格 (Ink Grid)**：针对长周期任务（如阅读、课程），采用可视化进度条。
    - **实心深色**：已完成的进度。
    - **水彩晕染**：进行中的进度。
- **批量管理**：提供高效的批量编辑视图，支持多选归档、移动分类等操作。

### 4. 📊 多维数据统计 (Analytics)
- **多视图切换**：支持日、周、月、年四个维度的统计视图。
- **Timeline 视图**：增强的每日时间轴，清晰展示一日的时间流向与碎片化程度。
- **Focus Chart**：类似 GitHub Contribution 的热力图，直观展示每日专注强度与平均专注分。
- **全屏沉浸模式**：提供大屏专注的数据分析面板。
- **Scope 洞察**：专门的领域分析视图，展示不同领域（如输入 vs 输出）的时间分配比例。

### 5. 🤖 AI 智能辅助 (AI Assistant)
- **自然语言速记**：直接输入自然语言（e.g.“昨天下午2点到4点看书，然后跑了1小时步”），AI 自动解析时间段并批量生成记录。
- **多模型支持**：内置支持 **Gemini**、**DeepSeek**、**硅基流动** 及所有兼容 OpenAI 格式的 API。
- **隐私保护**：API Key 本地存储，直接与服务商通信，不经过中间服务器。

### 6. 🏷️ 灵活标签系统 (Tag System)
- **二级分类结构**：Category（大分类） -> Activity（具体活动）。
- **Emoji 增强**：支持自动识别标签名称中的 Emoji 作为图标。
- **自定义主题**：每个分类可独立设置主题色（Tailwind 色系）。

### 7. ☁️ 数据安全与同步 (Data Sync)
- **WebDAV 同步**：内置 WebDAV 支持（如坚果云、Nextcloud），实现多端数据自动备份与同步。
- **数据管理**：
    - **导出/导入**：完整 JSON 数据备份与恢复。
    - **自动同步**：支持后台自动同步与启动时自动拉取。
- **本地优先**：所有数据优先存储于本地 SQLite/LocalStorage，从不上传至不明第三方服务器。

## 🛠️ 技术栈

本项目基于现代 Web 技术构建，并使用 Capacitor 封装为原生移动应用：

- **核心框架**: [React 19](https://react.dev/)
- **构建工具**: [Vite](https://vitejs.dev/)
- **移动端运行时**: [Capacitor 7](https://capacitorjs.com/) (Android)
- **语言**: [TypeScript](https://www.typescriptlang.org/)
- **样式**: [Tailwind CSS](https://tailwindcss.com/)
- **图标库**: [Lucide React](https://lucide.dev/)
- **数据同步**: [WebDAV Client](https://github.com/perry-mitchell/webdav-client)
- **AI 能力**: OpenAI Compatible API Client / Google GenAI SDK

## 📂 项目结构

```
src/
├── components/      # 通用 UI 组件 (Modal, Toast, FloatingTimer 等)
├── views/           # 主要页面视图
│   ├── RecordView.tsx       # 首页记录视图
│   ├── TimelineView.tsx     # 时间轴与日志管理
│   ├── StatsView.tsx        # 统计分析
│   ├── TodoView.tsx         # 待办事项列表
│   ├── ScopeView.tsx        # 领域概览
│   ├── ScopeDetailView.tsx  # 领域详情与目标分析
│   ├── FocusDetailView.tsx  # 专注度详情
│   └── SettingsView.tsx     # 设置与数据管理
├── services/        # 核心服务逻辑 (webdav, aiService 等)
├── constants.ts     # 全局常量与预设数据
├── types.ts         # TypeScript 类型定义 (Scope, Goal, Log, Todo 等)
└── App.tsx          # 应用入口与路由逻辑
android/             # Android 原生工程目录 (Generated by Capacitor)
```

## 🚀 快速开始

### 开发环境准备

1. **Node.js**: 确保已安装 Node.js 环境。
2. **Android Studio**: (仅移动端开发需要) 安装 Android Studio 以编译 APK。

### 安装依赖

```bash
npm install
```

### 启动 Web 开发服务器

```bash
npm run dev
```

### 📱 Android 应用开发与打包

本项目使用 **Capacitor** 将 Web 应用打包为 Android APK。

#### 1. 初始化环境 (首次)
```bash
# 如果尚未添加 android 平台
npx cap add android
```

#### 2. 构建流程
每次修改 React 代码后，若要更新到 Android 应用中，请遵循以下步骤：

1. **构建 Web 资源**:
   ```bash
   npm run build
   ```
   *这将生成 `dist` 目录。*

2. **同步到 Android 项目**:
   ```bash
   npx cap sync
   ```
   *这将把 `dist` 目录的内容复制到 `android/app/src/main/assets/public`，并更新原生插件。*

3. **打开 Android Studio 进行编译/调试**:
   ```bash
   npx cap open android
   ```
   *在 Android Studio 中打开后，点击绿色的 "Run" ▶️ 按钮即可在模拟器或真机上运行。*

### 🌐 Vercel 部署

本项目支持一键部署到 Vercel,并通过 Serverless 函数解决 WebDAV 跨域问题。

#### 快速部署

1. **使用 Vercel Dashboard**:
   - 访问 [Vercel](https://vercel.com)
   - 导入你的 Git 仓库
   - Vercel 会自动检测配置并部署

2. **使用命令行**:
   ```bash
   # 安装 Vercel CLI
   npm install -g vercel
   
   # 登录并部署
   vercel
   
   # 或使用快速部署脚本
   ./deploy.ps1
   ```

#### WebDAV 跨域解决方案

在浏览器环境中,WebDAV 请求会遇到 CORS 限制。项目已配置:
- **开发环境**: 通过 Vite 代理转发请求
- **生产环境**: 通过 Vercel Serverless 函数 (`/api/webdav-proxy`) 代理请求
- **原生应用**: 使用原生 HTTP 客户端,无 CORS 限制

详细部署指南请查看: [VERCEL_DEPLOY.md](./VERCEL_DEPLOY.md)


---


