<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_todo_pin_row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="false"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:minHeight="28dp"
        android:orientation="horizontal"
        android:paddingStart="0dp"
        android:paddingEnd="2dp"
        android:paddingTop="2dp"
        android:paddingBottom="3dp">

        <TextView
            android:id="@+id/widget_todo_pin_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:fontFamily="sans-serif-light"
            android:maxLines="1"
            android:text="待办标题"
            android:textColor="#2E2822"
            android:textSize="12sp"
            android:textStyle="normal" />

        <FrameLayout
            android:id="@+id/widget_todo_pin_action_button"
            android:layout_width="34dp"
            android:layout_height="24dp"
            android:clickable="true"
            android:focusable="false">

            <ImageView
                android:id="@+id/widget_todo_pin_button_start"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:padding="5dp"
                android:scaleType="centerInside"
                android:src="@drawable/widget_todo_pin_play_icon"
                android:tint="#1F1B16" />

            <ImageView
                android:id="@+id/widget_todo_pin_button_stop"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:padding="5dp"
                android:scaleType="centerInside"
                android:src="@drawable/widget_todo_pin_stop_icon"
                android:tint="#1F1B16"
                android:visibility="gone" />

            <ImageView
                android:id="@+id/widget_todo_pin_button_open"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:padding="5dp"
                android:scaleType="centerInside"
                android:src="@drawable/widget_todo_pin_play_icon"
                android:tint="#1F1B16"
                android:visibility="gone" />
        </FrameLayout>
    </LinearLayout>

    <TextView
        android:id="@+id/widget_todo_pin_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#14362C21" />
</LinearLayout>
