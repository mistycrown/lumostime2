# 配色方案迁移清单

## 📋 迁移优先级

### ✅ 已完成
- [x] FloatingButton 组件
- [x] CSS 变量系统搭建
- [x] 配色选择器

### 🎯 高优先级（建议先做）

#### 通用组件
- [ ] 按钮组件
  - [ ] 主要按钮 (bg-stone-900) → btn-primary
  - [ ] 次要按钮 (bg-white border) → btn-secondary
  - [ ] 图标按钮 → icon-button

- [ ] 卡片组件
  - [ ] TodoItem
  - [ ] ActivityItem  
  - [ ] GoalCard
  - [ ] TimelineItem

- [ ] 输入组件
  - [ ] 文本输入框 → input
  - [ ] 文本域 → input
  - [ ] 选择器

### 📦 中优先级（按需迁移）

#### 页面组件
- [ ] TimelineView
  - [ ] 时间线卡片
  - [ ] 日期选择器
  
- [ ] TodoView
  - [ ] 待办卡片
  - [ ] 分类标签

- [ ] TagsView / ScopeView
  - [ ] 标签卡片
  - [ ] 统计卡片

- [ ] StatsView
  - [ ] 图表容器
  - [ ] 数据卡片

### 🔄 低优先级（保持现状）

- [ ] 设置页面
- [ ] 详情页面
- [ ] 模态框

## 🛠️ 迁移方法

### 方法 1：简单替换（最快）

```tsx
// 之前
<button className="px-4 py-2 bg-stone-900 text-white rounded-lg">
  保存
</button>

// 之后
<button className="btn-primary">
  保存
</button>
```

### 方法 2：使用 CSS 变量

```tsx
// 之前
<div className="bg-white border border-stone-200 rounded-lg p-4">

// 之后
<div className="card">
```

### 方法 3：混合使用

```tsx
// 保留 Tailwind 的布局类，只替换颜色
<button className="btn-primary flex items-center gap-2">
  <Icon />
  <span>保存</span>
</button>
```

## 📝 迁移模板

### 按钮迁移

| 旧样式 | 新样式 | 说明 |
|--------|--------|------|
| `bg-stone-900 text-white` | `btn-primary` | 主要按钮 |
| `bg-white border border-stone-200` | `btn-secondary` | 次要按钮 |
| `bg-transparent text-stone-600` | `btn-ghost` | 幽灵按钮 |

### 卡片迁移

| 旧样式 | 新样式 | 说明 |
|--------|--------|------|
| `bg-white border border-stone-200 rounded-lg p-4` | `card` | 标准卡片 |
| `bg-white border border-stone-200 rounded-lg` | `card-flat` | 扁平卡片 |
| `bg-white rounded-lg shadow-sm p-4` | `card-elevated` | 悬浮卡片 |

### 输入框迁移

| 旧样式 | 新样式 | 说明 |
|--------|--------|------|
| `bg-white border border-stone-200 px-3 py-2 rounded-lg` | `input` | 标准输入框 |

## 🎯 迁移建议

### 1. 不要一次性全改
- 选择一个高频组件开始
- 测试没问题后再继续下一个

### 2. 保持向后兼容
- 新旧代码可以共存
- 不着急的地方保持现状

### 3. 优先迁移通用组件
- 改一次影响多个页面
- 性价比最高

### 4. 记录迁移进度
- 在这个清单中打勾
- 方便追踪进度

## 🔍 查找需要迁移的代码

### 查找按钮
```bash
# 查找主要按钮
grep -r "bg-stone-900.*text-white" src/

# 查找次要按钮  
grep -r "bg-white.*border.*border-stone" src/
```

### 查找卡片
```bash
grep -r "bg-white.*border.*rounded-lg.*p-" src/
```

### 查找输入框
```bash
grep -r "border.*border-stone.*px-.*py-.*rounded" src/
```

## ⚠️ 注意事项

1. **测试配色切换**
   - 迁移后测试 default 和 purple 两个配色
   - 确保样式正确

2. **保留必要的 Tailwind 类**
   - 布局类（flex, grid, gap 等）保留
   - 只替换颜色相关的类

3. **特殊情况处理**
   - 有些组件可能需要特殊颜色
   - 可以继续使用 Tailwind 类

## 📊 进度追踪

- 已完成：1 / 20+
- 进度：~5%
- 预计工作量：按需迁移，无固定时间表

## 🎓 参考文档

- 快速参考：`QUICK-THEME-REFERENCE.md`
- 详细指南：`theme-system-guide.md`
- CSS 变量定义：`src/styles/themes.css`
