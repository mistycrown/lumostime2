# 脉络子任务展开入口布局设计

## 目标

确保脉络右侧待办栏中，有直接子任务的父任务始终能显示展开/收起入口。

## 方案

- 保留既有父子树分组和默认收缩状态。
- 将任务行的外层从固定满宽改为可收缩的弹性项，并添加 `min-w-0`。
- 展开按钮保留固定尺寸，使其不会被任务标题、`PIN` 或外层裁切挤出可视区域。

## 验证

- 保留既有树分组回归测试。
- 运行侧栏测试与生产构建，并检查窄侧栏布局不发生溢出。
