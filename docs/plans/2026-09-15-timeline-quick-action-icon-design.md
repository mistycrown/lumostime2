# 脉络页快捷按钮图标区分设计

## 背景

脉络页顶部的“回顾总览”和“原则库”快捷按钮都使用 `BookOpen`，导致入口难以通过图标快速区分。

## 方案

保留“回顾总览”的 `BookOpen`，将“原则库”改为 Lucide `Scale`。`Scale` 更直接表达原则、准则与权衡语义，同时保持现有按钮尺寸、标签和导航行为不变。

## 验证

运行 `npm run build`，确认 TypeScript 编译与生产构建通过。
