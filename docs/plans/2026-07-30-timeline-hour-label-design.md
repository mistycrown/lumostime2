# 脉络时间轴整点标签设计

## 目标

压缩脉络时间轴左侧刻度的横向占用，只显示整点小时数字。

## 方案

- 保留 00:00 至 24:00 的完整网格线。
- 隐藏顶部 00:00 的标签文字。
- 01:00 至 24:00 的刻度分别显示为两位数 `01` 至 `24`，不再显示 `:00`。
- 时间块内的起止时间继续显示小时与分钟，不受刻度标签样式影响。

## 验证

- 为刻度标签格式增加单元测试。
- 运行时间轴测试和生产构建。
