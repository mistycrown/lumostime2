# 脉络待办布局手势隔离设计

## 目标

避免“时间轴 + 待办”布局中从待办侧栏拖动任务时，被脉络页根层左右滑动换天手势抢占。

## 方案

- 仅在 `timeline-todo` 布局下禁用根层横向触摸换天的起止坐标记录和触发逻辑。
- 纯时间轴布局继续保留现有左右换天。
- 日期导航按钮、日历选择、时间轴上下滚动、双指缩放和待办 Pointer 拖拽不受影响。

## 验证

- 运行时间轴相关测试和生产构建。
- 手动验证时间轴 + 待办布局内横向拖动任务不会改变日期。
