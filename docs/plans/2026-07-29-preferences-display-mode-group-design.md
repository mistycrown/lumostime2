# 偏好设置显示模式分组调整

## 目标

将“显示模式”从独立分组移入“通用”分组，并作为该分组第一项。

## 设计

- 复用现有的主题模式下拉菜单、状态和选项。
- 删除单独的“显示模式”分组标题和容器。
- 将显示模式行移至“通用”卡片顶部，以分隔线与隐私模式区分。
- 通用卡片允许下拉菜单浮出，避免选项被容器裁切。

## 验证

运行生产构建，确认 TypeScript 编译和打包通过。
