# 关键词提示图标对齐设计

## 目标

让关键词标题右侧的提示图标与“关键字”标题文字垂直居中对齐。

## 方案

仅调整 `TagDetailView` 关键词区标题行的 Flex 对齐方式，将 `items-end` 改为 `items-center`。保留共享 `FeatureHint` 组件及其尺寸、交互和其他页面布局不变，避免引入与本问题无关的视觉变化。

## 验证

运行 `npm run build`，并检查变更 diff 确认只包含该布局修复、设计记录和源文件头注释更新。
