# 脉络数字日课计数可见性设计

## 目标

让数字类型日课在每次点击后立即显示当前计数：`1`、`2`、`3`，达到目标后的下一次点击显示空圆圈。

## 方案

- 保留既有的日课数据更新逻辑：未完成时递增，已达到目标时重置为零。
- 将侧栏标记的透明文字样式限制为普通未完成勾选类型；数字类型无论是否达到目标都使用可见文字颜色。
- 不改变自动日课和普通手动日课的方形、圆形标记规则。

## 验证

- 增加侧栏数字标记的可见性判定回归测试。
- 运行日课与时间轴侧栏相关测试及生产构建。
