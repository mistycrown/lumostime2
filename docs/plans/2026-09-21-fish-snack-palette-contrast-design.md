# 投喂小鱼干配色末色对比度调整设计

## 背景

“雾感莫兰迪”和“春日东方”的最后一个颜色接近白色，在投喂小鱼干的配色序列预览和统计图中不易识别。

## 设计

- 将“雾感莫兰迪”的最后一个颜色从 `#E8E4DF` 调整为 `#8C9A92`，使用低饱和灰绿色保持莫兰迪的克制感。
- 将“春日东方”的最后一个颜色从 `#F8F9FA` 调整为 `#D9B08C`，使用柔和杏棕补充春日东方的暖色气质。
- 保持其他配色、主题色、布局和序列顺序不变。

## 验证

- 更新 `chartPalette.test.ts` 中的精确序列断言。
- 执行配色单元测试和生产构建。
