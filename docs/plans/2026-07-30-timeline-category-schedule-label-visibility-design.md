# 脉络分类待办排期标签显示设计

## 目标

脉络右侧的“今日”分组继续显示排期类型标签；具体任务列表不展示 `ARR`、`DUE` 等排期标签，仅在任务置顶时显示 `PIN`。

## 方案

- 保留待办条目的现有排期数据和标签计算逻辑，避免改动排期模型。
- `TodoRow` 新增显示上下文：仅“今日”列表允许展示排期类型标签。
- 具体分类列表关闭排期标签；置顶状态始终优先展示 `PIN`。

## 验证

- 增加单元测试，确认分类列表隐藏 `ARR`，置顶项仍显示 `PIN`。
- 运行侧栏测试和生产构建。
