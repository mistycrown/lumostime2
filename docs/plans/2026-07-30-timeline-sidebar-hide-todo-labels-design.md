# 脉络待办栏标签隐藏设计

## 目标

在窄宽度的脉络右侧待办栏中，为任务标题保留更多可用空间。

## 方案

- 不在右侧待办栏渲染任何尾部标签。
- 同时隐藏排期类型标签（`DUE / ARR / REP / MAY / DONE`）和置顶标签（`PIN`）。
- 排期、置顶、排序和任务交互的数据逻辑保持不变，仅移除这一个视图的视觉标记。

## 验证

- 更新标签解析测试，确保所有待办条目在右侧栏均不产生标签。
- 运行侧栏测试和生产构建。
