# 投喂小鱼干配色序列平铺样式设计

## 范围

移除“投喂小鱼干”中每个预设和自定义色彩序列项的卡片边框，保留现有两列/三列网格、色带、名称、序列类型和自定义序列点击编辑行为。

## 方案

仅调整 `ChartPaletteSequenceManager` 中序列项的 Tailwind 样式，删除序列项的边框类；整体设置面板、新建按钮、编辑弹窗和数据逻辑保持不变。

## 验证

运行生产构建，确认 TypeScript、Vite 打包和现有交互入口不受影响。
