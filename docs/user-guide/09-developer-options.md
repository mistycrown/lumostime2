# 开发者选项

## 隐私模式 (Privacy / Mosaic Mode)
在演示或截图时，如果需要隐藏敏感信息（如备注、个人信息），可以使用隐私模式：
1. 打开控制台 (开发者工具 -> Console)
2. 输入 `window.togglePrivacyMode()` 并回车
3. 界面中的敏感文字将变为模糊状态
4. 再次输入该命令即可解除模糊

---

*LumosTime - Illuminate your time, illuminate your life.* ✨
