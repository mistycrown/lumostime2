# Water 贴纸集配置完成

## 配置信息

已成功将 `public/sticker/water` 文件夹中的 16 张贴纸配置到系统中。

### 贴纸集详情

- **ID**: `water`
- **名称**: Water 水彩
- **描述**: 清新的水彩风格贴纸
- **图片数量**: 16 张
- **图片格式**: PNG
- **路径**: `/sticker/water/01.png` ~ `/sticker/water/16.png`

## 配置位置

文件：`src/services/stickerService.ts`

```typescript
{
    id: 'water',
    name: 'Water 水彩',
    description: '清新的水彩风格贴纸',
    isCustom: false,
    stickers: [
        { path: '/sticker/water/01.png' },
        { path: '/sticker/water/02.png' },
        { path: '/sticker/water/03.png' },
        { path: '/sticker/water/04.png' },
        { path: '/sticker/water/05.png' },
        { path: '/sticker/water/06.png' },
        { path: '/sticker/water/07.png' },
        { path: '/sticker/water/08.png' },
        { path: '/sticker/water/09.png' },
        { path: '/sticker/water/10.png' },
        { path: '/sticker/water/11.png' },
        { path: '/sticker/water/12.png' },
        { path: '/sticker/water/13.png' },
        { path: '/sticker/water/14.png' },
        { path: '/sticker/water/15.png' },
        { path: '/sticker/water/16.png' },
    ]
}
```

## 使用方法

### 1. 在 MoodPicker 中选择

1. 打开心情选择器（点击日历上的日期）
2. 使用左右箭头或页码指示器切换到 "Water 水彩" 页面
3. 点击任意贴纸进行选择

### 2. 页面顺序

- **页面 1**: Emoji 组
- **页面 2**: Water 水彩（新添加）
- **页面 3**: 水彩风格
- **页面 4**: 像素风格

### 3. 存储格式

选择 Water 贴纸后，数据存储格式：

```typescript
// 例如选择第一张贴纸
dailyReview.moodEmoji = "image:/sticker/water/01.png";

// 选择第五张贴纸
dailyReview.moodEmoji = "image:/sticker/water/05.png";
```

### 4. 在心情月历中显示

心情月历会自动识别并渲染贴纸：

```typescript
// MoodCalendar 组件会自动处理
<IconRenderer icon="image:/sticker/water/01.png" size="100%" />
```

## 测试步骤

### 1. 启动应用

```bash
npm run dev
```

### 2. 打开心情月历

导航到 Journal 视图，找到心情月历组件。

### 3. 选择贴纸

1. 点击任意日期格子
2. 在弹出的选择器中，点击右箭头切换到 "Water 水彩" 页面
3. 选择任意一张贴纸
4. 关闭选择器

### 4. 验证显示

- 选择的贴纸应该显示在日历格子中
- 贴纸应该自适应格子大小
- 再次点击该日期，选择器应该显示该贴纸为选中状态（圆形边框）

## 预期效果

### 在选择器中

- 16 张贴纸以 4x4 网格排列
- 每张贴纸占据一个格子
- 无标签显示（视觉简洁）
- 选中的贴纸有圆形边框

### 在月历中

- 贴纸自适应格子大小（占据 60% 空间）
- 贴纸居中显示
- 日期数字显示在底部

## 故障排查

### 贴纸不显示

1. **检查路径**：确保路径是 `/sticker/water/01.png`（以 `/` 开头）
2. **检查文件**：确认 `public/sticker/water/` 文件夹中有对应的 PNG 文件
3. **检查控制台**：打开浏览器开发者工具，查看是否有 404 错误

### 贴纸显示错误

1. **清除缓存**：刷新浏览器（Ctrl+F5 或 Cmd+Shift+R）
2. **检查格式**：确认图片格式为 PNG
3. **检查大小**：确认图片文件大小合理（不要太大）

### 选择器不显示贴纸页面

1. **检查导入**：确认 `MoodPicker.tsx` 导入了 `stickerService`
2. **检查配置**：确认 `stickerService.ts` 中的 `PRESET_STICKER_SETS` 包含 water 配置
3. **重启应用**：停止并重新启动开发服务器

## 数据示例

### 选择 Water 贴纸后的数据

```json
{
  "date": "2024-01-15",
  "moodEmoji": "image:/sticker/water/03.png",
  "content": "今天心情不错",
  "tags": ["工作", "学习"]
}
```

### 混合使用 Emoji 和 Sticker

```json
[
  {
    "date": "2024-01-01",
    "moodEmoji": "😊"
  },
  {
    "date": "2024-01-02",
    "moodEmoji": "image:/sticker/water/01.png"
  },
  {
    "date": "2024-01-03",
    "moodEmoji": "🥰"
  },
  {
    "date": "2024-01-04",
    "moodEmoji": "image:/sticker/water/05.png"
  }
]
```

## 下一步

### 添加更多贴纸集

如果要添加更多贴纸集，只需：

1. 在 `public/sticker/` 下创建新文件夹
2. 添加图片文件
3. 在 `stickerService.ts` 的 `PRESET_STICKER_SETS` 中添加配置

例如添加 "kawaii" 贴纸集：

```typescript
{
    id: 'kawaii',
    name: 'Kawaii 可爱',
    description: '超可爱的卡通风格',
    isCustom: false,
    stickers: [
        { path: '/sticker/kawaii/01.png' },
        { path: '/sticker/kawaii/02.png' },
        // ...
    ]
}
```

### 自定义贴纸标签

如果需要为某些贴纸添加标签：

```typescript
stickers: [
    { path: '/sticker/water/01.png', label: 'Happy' },
    { path: '/sticker/water/02.png', label: 'Sad' },
    { path: '/sticker/water/03.png' }, // 无标签
    // ...
]
```

## 完成状态

✅ 配置完成
✅ 16 张贴纸已添加
✅ 可在选择器中使用
✅ 可在月历中显示
✅ 支持与 Emoji 混合使用
