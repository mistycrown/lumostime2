# LumosTime 快速入门指南 

欢迎使用 **LumosTime**！这不仅仅是一个时间记录工具，更是一个帮助你将被动的时间流逝转化为主动生命积累的私人助手。

本指南将帮助你快速上手核心功能。

---

## 1. ⏱️ 开始记录你的时间

LumosTime 的核心理念是“无感记录”。我们尽量简化了开始和结束的步骤。

### 方式一：点击即录 (推荐)
1. 打开应用首页 (**Record Tab**)。
2. 左侧栏选择 **大类 (Category)**（如：Work, Learning, Life...）。
3. 右侧网格中点击具体的 **活动图标 (Activity)**。
   - **效果**：屏幕右下角会出现一个**悬浮计时球**，表示计时已开始。
   - **结束**：点击悬浮球，选择停止 (Stop)。
   - **专注度 (Focus Score)**：停止时，你还可以对本次专注质量进行 **1-5 分** 的打分（需在标签/分类设置中开启），帮助你追踪状态波动。

### 方式二：手动补录
如果你忘记了实时记录，可以在 **Timeline** 页面补录：
1. 切换到底部导航栏的 **Timeline**。
2. 点击右下角的 **+** 按钮。
3. 在弹出的 **Add Log** 窗口中，你可以：
   - 滑动时间块来选择时间段。
   - 选择分类标签、关联待办、关联领域。
   - 填写备注。

### 方式三：AI 智能速记
不想手动选时间？让 AI 帮你。
1. 在 **Timeline** 页面左下角，点击 **AI 图标** (魔法星星)。
2. 输入自然语言，例如：
   > "今天早上9点到11点写代码，中午睡了半小时午觉，下午2点开始开了1个小时会。"
3. AI 会通过内置模型（支持 OpenAI/Gemini/DeepSeek）自动解析并通过 JSON 格式生成多条时间记录供你确认。（你需要先在设置页填写自定义API）

---

### ⚡ 任务即快捷入口
任务不仅仅是待办清单，它们也是你记录时间的**快捷入口**。
- 你可以将任务预先**关联到特定的活动标签**（如 "Reading"）或 **领域**（如 "Input"）。
- 这样，你只需在任务右侧点击 **播放 (Play)** 按钮 ▶️，系统就会自动按预设配置开始计时。
- **优势**：对于经常重复发生的事件（如“背单词”、“锻炼”），你无需每次开始时都去选择分类和标签，直接点击任务即可一键开始。
- 当你停止计时时，这段时间会自动计入该任务的 **进度条 (Progress/Ink Mode)** 中（前提是你已为该任务开启了 Progress Tracking）。
- 快捷操作：
  - 向左滑动任务卡片 👈 完成/撤销完成任务
  - 向右滑动任务卡片 👉 为这个任务创建一个副本
---

## 3. 🎯 领域与目标 (Scope & Goals)

超越简单的分类标签，我们引入了 **Scope** 概念来从更高维度审视生活。

- 什么是 Scope？
  - Scope 是跨分类的属性，用于从另一个维度聚合你的时间。
  - **示例 1：能量流向**
    - “写代码”（工作）、“写作”（兴趣） -> **Output (输出)**
    - “看书”（学习）、“听播客”（休闲） -> **Input (输入)**
  - **示例 2：学科/技能维度**
    - “背单词”（学习）、“看美剧”（娱乐） -> **English (英语)**
    - “健身”（运动）、“冥想”（生活） -> **Health (健康)**

- **设定目标**
  1. 进入 **Scope** 页面。
  2. 你可以为某个 Scope 设定周期性目标（支持 5 种指标类型）：
     - **原始时长 (Raw Duration)**：累计投入多少小时（如：100小时编程）。
     - **有效时长 (Weighted Duration)**：基于专注度加权的时长（如：高质量阅读 50 小时）。
     - **待办数量 (Task Count)**：完成多少个关联任务（如：刷完 200 道力扣题）。
     - **活跃天数 (Frequency)**：养成习惯，坚持打卡多少天（如：坚持晨跑 30 天）。
     - **时长上限 (Duration Limit)**：限制“坏习惯”的时间（如：每月游戏不超过 10 小时）。
  3. 系统会自动计算所有 **关联到该 Scope 的时间记录** 来更新进度。

---

## 4. 📊 多维数据回顾

LumosTime 提供从微观到宏观的全方位数据复盘。

### 全局统计 (Stats Tab)
- **多维度占比图 (Time Distribution)**：支持切换 **日 / 周 / 月 / 年** 四个维度，通过圆环图（Pie Chart）清晰展示各类活动的时间占比。
- **周时间轴 (Weekly Timeline)**：纵览一周的时间流向，快速发现作息规律与异常。

### 专项分析 (Detail Views)
在每个**标签 (Tag)**、**领域 (Scope)** 或 **待办 (Todo)** 的详情页中，都有专属的深度图表：
- **投入热力图 (Time Heatmap)**：类似 GitHub Contribution，直观展示你在该项目上的长期投入频率。
- **专注热力图 (Focus Heatmap)**：颜色深浅代表专注分的高低，一眼识别“心流”时刻。
- **专注堆积图 (Focus Stacked)**：分析不同专注等级（1-5分）的时长构成与趋势。

### 每日复盘 (Timeline Tab)
- **Timeline**：精确到分钟的一日时间轴，帮助你检查是否存在大量碎片化时间。

### 📤 导出报告 (Export to Text)
LumosTime 支持将数据以文本格式导出，方便你粘贴到 Notion、Obsidian 或周报中：
- **导出日报**：在 Timeline 页面底部，点击“导出日报”，即可复制当日详细记录（含时间段、分类、备注、专注分）。
- **导出统计**：在 Stats 页面底部，点击“导出统计”，即可复制当前维度的类别占比汇总。

---

## 5. ☁️ 数据同步与安全

LumosTime 坚持 **Local-First (本地优先)** 策略。
- 你的数据默认只保存在手机本地。
- **云同步**：我们支持 **WebDAV** 协议。
  - 你可以使用 坚果云、Nextcloud 或任何支持 WebDAV 的网盘。
  - 在 **Settings** -> **Data & Sync** 中配置。
  - 配置后，数据将在多端（Web / Android）自动同步。

---

*LumosTime - Illuminate your time, illuminate your life.*

*开始掌控你的时间吧！*


