# 状态栏自适应背景功能 - 更新说明

## 问题描述

之前应用的状态栏颜色是固定的白色，当用户更改背景图片后，状态栏与应用标题栏之间会出现视觉割裂，无法融为一体。

## 解决方案

实现了状态栏自适应背景功能，根据背景图片的颜色自动调整状态栏图标的颜色。

## 技术实现

### 1. 新增服务：`statusBarService.ts`

创建了专门的状态栏管理服务，提供以下功能：

- **图片分析**：分析背景图片顶部区域的颜色和亮度
- **智能调整**：根据背景亮度自动选择合适的状态栏样式
  - 深色背景（亮度 < 0.5）→ 浅色图标（白色）
  - 浅色背景（亮度 ≥ 0.5）→ 深色图标（黑色）
- **性能优化**：使用采样算法，只分析必要的像素点

### 2. 更新服务：`backgroundService.ts`

集成状态栏服务，在以下场景自动更新状态栏：

- 切换背景图片时
- 调整背景透明度时
- 应用初始化时

### 3. 更新组件：`ImmersiveTimer.tsx`

优化沉浸式计时器的状态栏处理：

- 进入沉浸式模式时，使用主题对应的状态栏样式
- 退出沉浸式模式时，恢复到背景服务管理的状态栏样式（而不是硬编码的白色）

## 使用方式

功能已自动集成，用户无需任何额外操作：

1. 在「个性化设置」中选择背景图片
2. 状态栏会自动调整为合适的颜色
3. 切换不同背景时，状态栏会实时更新

## 兼容性

- ✅ Android 设备
- ✅ iOS 设备
- ⚠️ Web 浏览器（自动跳过，不影响功能）

## 调试方法

在浏览器控制台或 Android/iOS 调试工具中：

```javascript
// 查看当前状态栏样式
statusBarService.getCurrentStyle()

// 手动测试特定背景
statusBarService.updateForBackground('/background/night.webp')

// 重置状态栏
statusBarService.reset()

// 强制重新应用背景
backgroundService.forceApplyBackground()
```

## 文件清单

### 新增文件
- `src/services/statusBarService.ts` - 状态栏管理服务
- `docs/status-bar-integration.md` - 技术文档
- `docs/user-guide/11-status-bar-adaptation.md` - 用户指南

### 修改文件
- `src/services/backgroundService.ts` - 集成状态栏服务
- `src/components/ImmersiveTimer.tsx` - 优化状态栏恢复逻辑

## 测试建议

1. **基础测试**
   - 切换不同亮度的背景图片
   - 验证状态栏图标颜色是否正确调整

2. **边界测试**
   - 使用极深色背景（如纯黑）
   - 使用极浅色背景（如纯白）
   - 使用中等亮度背景

3. **场景测试**
   - 进入/退出沉浸式计时器
   - 调整背景透明度
   - 上传自定义背景图片

4. **性能测试**
   - 快速切换多个背景
   - 验证是否有卡顿或延迟

## 注意事项

1. 状态栏分析只针对图片顶部区域，确保与实际显示位置匹配
2. 如果图片加载失败，会自动回退到默认浅色状态栏
3. 沉浸式计时器有独立的状态栏管理，不受背景服务影响
4. 服务已暴露到全局对象（开发环境），便于调试

## 后续优化建议

1. 可以考虑添加手动覆盖选项，让用户强制指定状态栏颜色
2. 可以缓存分析结果，避免重复分析相同的背景图片
3. 可以提供预览功能，让用户在选择背景前看到状态栏效果
