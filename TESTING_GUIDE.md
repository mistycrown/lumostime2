# 状态栏透明融合功能测试指南

## 前置条件

1. 在 Android 或 iOS 设备上运行应用
2. 确保已安装 `@capacitor/status-bar` 插件
3. 应用有权限修改状态栏样式

## 测试步骤

### 1. 基础透明功能测试

#### 测试状态栏透明
1. 打开应用设置
2. 进入「个性化设置」→「背景图片」
3. 选择任意背景图片
4. 观察状态栏是否变为透明，背景图片是否延伸到状态栏区域

#### 测试深色背景
1. 选择深色背景（如「夜晚」、「黑色」）
2. 观察状态栏图标是否变为白色
3. 确认背景图片延伸到状态栏区域

#### 测试浅色背景
1. 选择浅色背景（如「绿意」、「粉蓝」）
2. 观察状态栏图标是否变为黑色
3. 确认背景图片延伸到状态栏区域

#### 测试彩色背景
1. 选择彩色背景（如红色、蓝色系背景）
2. 观察状态栏是否完全透明
3. 确认背景颜色延伸到状态栏区域
4. 验证图标颜色是否清晰可见

### 2. 透明度调整测试

1. 选择任意背景图片
2. 调整背景透明度滑块
3. 验证状态栏保持透明
4. 验证图标颜色是否保持与背景匹配

### 3. 自定义背景测试

1. 点击「+」按钮上传自定义背景
2. 上传一张深色图片，验证：
   - 状态栏透明
   - 图标变为白色
3. 上传一张浅色图片，验证：
   - 状态栏透明
   - 图标变为黑色
4. 上传一张彩色图片（如红色、蓝色），验证：
   - 状态栏透明
   - 背景色延伸到顶部
   - 图标颜色清晰可见

### 4. 沉浸式计时器测试

1. 设置一个背景图片，确认状态栏透明
2. 开始一个计时任务
3. 进入沉浸式计时器模式
4. 观察状态栏是否变为不透明，使用主题颜色
5. 选择不同主题，观察状态栏是否跟随主题变化
6. 退出沉浸式模式
7. 验证状态栏是否恢复透明，并根据背景调整图标颜色

### 5. 页面切换测试

1. 设置一个背景图片
2. 在不同页面间切换（记录、待办、统计等）
3. 验证状态栏在所有页面保持透明
4. 验证图标颜色在所有页面保持一致

### 6. 应用重启测试

1. 设置一个背景图片
2. 完全关闭应用
3. 重新打开应用
4. 验证状态栏是否正确恢复透明状态
5. 验证图标颜色是否正确

## 预期结果

| 背景类型 | 状态栏背景 | 图标颜色 | 背景延伸 |
|---------|-----------|---------|---------|
| 默认 | 透明 | 黑色 | ✅ |
| 夜晚（深色） | 透明 | 白色 | ✅ |
| 黑色（深色） | 透明 | 白色 | ✅ |
| 绿意（浅色） | 透明 | 黑色 | ✅ |
| 粉蓝（浅色） | 透明 | 黑色 | ✅ |
| 红色（彩色） | 透明 | 黑/白 | ✅ |
| 蓝色（彩色） | 透明 | 黑/白 | ✅ |
| 自定义深色 | 透明 | 白色 | ✅ |
| 自定义浅色 | 透明 | 黑色 | ✅ |
| 沉浸式模式 | 不透明（主题色） | 黑/白 | ❌ |

## 调试方法

### 在浏览器控制台（开发模式）

```javascript
// 查看当前图标样式
console.log('当前图标样式:', statusBarService.getCurrentStyle())

// 查看当前背景
console.log('当前背景:', backgroundService.getCurrentBackgroundOption())

// 手动测试特定背景
await statusBarService.updateForBackground('/background/red.webp')

// 强制重新应用背景
backgroundService.forceApplyBackground()
```

### 在 Android Studio / Xcode 日志

查找以下日志输出：

```
✅ StatusBar service initialized with transparent background
🎨 StatusBar updated: brightness=0.XX, icons=Light (White)/Dark (Black)
🖼️ Background service initialized
```

## 常见问题排查

### 问题：状态栏没有变透明

**可能原因：**
1. 不在移动设备上运行（Web 浏览器会跳过）
2. StatusBar 插件未正确安装
3. 应用没有权限修改状态栏
4. 在沉浸式计时器模式中

**解决方法：**
```bash
# 检查插件是否安装
npm list @capacitor/status-bar

# 重新安装插件
npm install @capacitor/status-bar

# 同步到原生项目
npx cap sync
```

### 问题：背景没有延伸到状态栏

**可能原因：**
1. `setOverlaysWebView` 调用失败
2. 平台不支持透明状态栏

**解决方法：**
```javascript
// 在控制台手动测试
await StatusBar.setOverlaysWebView({ overlay: true })
```

### 问题：图标颜色不正确

**可能原因：**
1. 背景图片加载失败
2. 图片分析算法判断错误
3. 背景亮度处于临界值

**解决方法：**
```javascript
// 在控制台手动测试
await statusBarService.updateForBackground('你的背景URL')
```

### 问题：切换背景时有延迟

**可能原因：**
1. 图片文件过大
2. 网络加载慢
3. 图片分析需要时间

**解决方法：**
- 使用优化后的 WebP 格式图片
- 确保图片已缓存
- 等待图片完全加载

## 性能指标

- 背景切换响应时间：< 500ms
- 图片分析时间：< 200ms
- 状态栏更新时间：< 100ms
- 透明度设置时间：< 50ms

## 报告问题

如果发现问题，请提供以下信息：

1. 设备型号和操作系统版本
2. 应用版本
3. 使用的背景图片（如果是自定义背景）
4. 控制台日志输出
5. 重现步骤
6. 截图（显示状态栏效果）

## 前置条件

1. 在 Android 或 iOS 设备上运行应用
2. 确保已安装 `@capacitor/status-bar` 插件
3. 应用有权限修改状态栏样式

## 测试步骤

### 1. 基础功能测试

#### 测试深色背景
1. 打开应用设置
2. 进入「个性化设置」→「背景图片」
3. 选择深色背景（如「夜晚」、「黑色」）
4. 观察状态栏图标是否变为白色

#### 测试浅色背景
1. 在背景选择器中选择浅色背景（如「绿意」、「粉蓝」）
2. 观察状态栏图标是否变为黑色

#### 测试默认背景
1. 选择「默认」背景
2. 观察状态栏图标是否恢复为黑色（默认样式）

### 2. 透明度调整测试

1. 选择任意背景图片
2. 调整背景透明度滑块
3. 验证状态栏样式是否保持与背景匹配

### 3. 自定义背景测试

1. 点击「+」按钮上传自定义背景
2. 选择一张深色图片，验证状态栏变为白色图标
3. 选择一张浅色图片，验证状态栏变为黑色图标

### 4. 沉浸式计时器测试

1. 开始一个计时任务
2. 进入沉浸式计时器模式
3. 选择不同主题，观察状态栏是否跟随主题变化
4. 退出沉浸式模式
5. 验证状态栏是否恢复到与背景匹配的样式

### 5. 页面切换测试

1. 设置一个背景图片
2. 在不同页面间切换（记录、待办、统计等）
3. 验证状态栏样式在所有页面保持一致

### 6. 应用重启测试

1. 设置一个背景图片
2. 完全关闭应用
3. 重新打开应用
4. 验证状态栏样式是否正确恢复

## 预期结果

| 背景类型 | 背景亮度 | 状态栏图标颜色 |
|---------|---------|--------------|
| 默认 | N/A | 黑色 |
| 夜晚 | 低 | 白色 |
| 黑色 | 低 | 白色 |
| 绿意 | 高 | 黑色 |
| 粉蓝 | 高 | 黑色 |
| 自定义深色 | 低 | 白色 |
| 自定义浅色 | 高 | 黑色 |

## 调试方法

### 在浏览器控制台（开发模式）

```javascript
// 查看当前状态栏样式
console.log('当前样式:', statusBarService.getCurrentStyle())

// 查看当前背景
console.log('当前背景:', backgroundService.getCurrentBackgroundOption())

// 手动测试特定背景
await statusBarService.updateForBackground('/background/night.webp')

// 强制重新应用背景
backgroundService.forceApplyBackground()
```

### 在 Android Studio / Xcode 日志

查找以下日志输出：

```
✅ StatusBar service initialized
🎨 StatusBar updated: brightness=0.XX, style=Dark/Light
🖼️ Background service initialized
```

## 常见问题排查

### 问题：状态栏没有变化

**可能原因：**
1. 不在移动设备上运行（Web 浏览器会跳过）
2. StatusBar 插件未正确安装
3. 应用没有权限修改状态栏

**解决方法：**
```bash
# 检查插件是否安装
npm list @capacitor/status-bar

# 重新安装插件
npm install @capacitor/status-bar

# 同步到原生项目
npx cap sync
```

### 问题：状态栏颜色不正确

**可能原因：**
1. 背景图片加载失败
2. 图片分析算法判断错误

**解决方法：**
```javascript
// 在控制台手动测试
await statusBarService.updateForBackground('你的背景URL')
```

### 问题：切换背景时有延迟

**可能原因：**
1. 图片文件过大
2. 网络加载慢

**解决方法：**
- 使用优化后的 WebP 格式图片
- 确保图片已缓存

## 性能指标

- 背景切换响应时间：< 500ms
- 图片分析时间：< 200ms
- 状态栏更新时间：< 100ms

## 报告问题

如果发现问题，请提供以下信息：

1. 设备型号和操作系统版本
2. 应用版本
3. 使用的背景图片（如果是自定义背景）
4. 控制台日志输出
5. 重现步骤
